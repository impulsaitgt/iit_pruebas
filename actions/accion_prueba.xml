<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="accion_prueba" model="ir.actions.server" >
        <field name="name">Accion de Prueba</field>
        <field name="model_id" ref="libros"/>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="code">
            action = accion_prueba()
        </field>
    </record>

            <record id="model_account_move_action_share" model="ir.actions.server">
            <field name="name">Share</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_view_types">form</field>
            <field name="state">code</field>
            <field name="code">action = records.action_share()</field>
        </record>

</odoo>